apiVersion: v1
kind: Service
metadata:
  name: {{ include "gravivol.fullname" . }}
  labels:
    {{- include "gravivol.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gravivol.selectorLabels" . | nindent 4 }}
